const producto ={
    nombre:"Monitor 20 pulgadas",
    precio:300,
    disponible: true,
}

console.log(Object.keys(producto)); //Las llaves
console.log(Object.values(producto)); //Los valores
console.log(Object.entries(producto)); //Todo en pares